# Quasar Hub — configuración (modo nativo).
WEB_HOST=0.0.0.0
WEB_PORT=8080
WEB_DEBUG=true

# Archivo de flags que el Hub edita (en Docker se monta desde infra/compose).
QUASAR_ENV_FILE=../../infra/compose/.env.docker
