# Web
fastapi==0.115.*
uvicorn[standard]==0.34.*

# MongoDB (resultados de las agregaciones del stream)
pymongo==4.11.*
motor==3.7.*

# Spark Structured Streaming: el motor del laboratorio.
pyspark==3.5.*  # Must be 3.5.x — connectors don't support 4.x yet
numpy==1.26.*
pyarrow==15.*
pandas==2.2.*

# Config
python-dotenv==1.1.*

# Utils
pydantic==2.11.*
