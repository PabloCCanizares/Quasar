# Web
fastapi==0.115.*
uvicorn[standard]==0.34.*

# MongoDB (almacenamiento de metadatos del corpus y métricas)
pymongo==4.11.*
motor==3.7.*

# Neo4j (Fase posterior: grafo de relaciones entre robots/sensores/eventos)
neo4j==5.28.*

# Spark (engine del Tema 5: preprocesamiento masivo)
pyspark==3.5.*  # Must be 3.5.x — connectors don't support 4.x yet
numpy==1.26.*
pyarrow==15.*
pandas==2.2.*

# Machine learning para imputación / clustering / feature selection
# (sklearn es más eficiente que Spark MLlib para datasets <100k filas que
# es lo que manejamos aquí; se mantiene fidelidad pedagógica al PDF del
# Tema 5 — mismas técnicas, distinto motor)
scikit-learn==1.5.*

# Config
python-dotenv==1.1.*

# Utils
pydantic==2.11.*
