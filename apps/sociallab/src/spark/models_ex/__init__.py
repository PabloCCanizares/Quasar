"""Scaffolds de los modelos ML.

Cada archivo es un ejercicio. La firma publica (`train()` o
`build_recommendations()`) se mantiene identica a la solucion en
`src/spark/models/` para que `run_all.py` pueda importar uno u otro
segun la variable `LAB_ML`.
"""
