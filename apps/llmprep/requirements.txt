# Web
fastapi==0.115.*
uvicorn[standard]==0.34.*

# MongoDB (metadatos del corpus + métricas)
pymongo==4.11.*
motor==3.7.*

# Neo4j (grafo de near-duplicates en bloque dedup)
neo4j==5.28.*

# Spark (limpieza masiva del corpus)
pyspark==3.5.*
numpy==1.26.*
pyarrow==15.*
pandas==2.2.*

# Config
python-dotenv==1.1.*
pydantic==2.11.*

# NLP / LLM (se usan en fases posteriores)
# - datasets: descarga de Wikipedia ES (Fase 13)
# - tokenizers: BPE (Fase 16)
# - torch: nanoGPT (Fase 17)
# Se instalan bajo demanda para no inflar la imagen base del esqueleto.
